name: CI login-mfe

on:
  push:
    branches: [main]
    paths:
      - "frontend/login-mfe/**"
      - ".github/workflows/ci-login-mfe.yml"
  pull_request:
    branches: [main]
    paths:
      - "frontend/login-mfe/**"
      - ".github/workflows/ci-login-mfe.yml"

jobs:
  build-login-mfe:
    name: Build login-mfe
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend/login-mfe

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: frontend/login-mfe/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Show TypeScript version
        run: npx tsc -v

      - name: Build login-mfe
        run: npm run build
