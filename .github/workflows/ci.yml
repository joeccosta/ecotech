name: Legacy CI placeholder

on:
  workflow_dispatch:

jobs:
  legacy-ci-placeholder:
    name: Legacy CI placeholder
    runs-on: ubuntu-latest

    steps:
      - name: Explain workflow split
        run: echo "CI pipelines were split into ci-users.yml, ci-orders.yml, ci-shell.yml and ci-orders-mfe.yml"